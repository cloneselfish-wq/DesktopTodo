# 更新日志 (Changelog)

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式记录变更。

## [1.1.0] - 2026-09-07

### Changed

- 全新现代化界面：白色标题栏（主题色圆点 + 待办计数"N 项"）替代蓝色渐变，图标按钮改为灰色幽灵按钮（关闭键悬停变红）
- 添加卡片重做：无边框输入框 + 圆形"＋"按钮；截止日期改为胶囊按钮，点击弹出日期选择，选中后可在胶囊上直接清除
- 待办卡片：圆角方形勾选框（完成后填充主题色）、截止日期彩色胶囊徽标（过期红 / 今天橙 / 其他灰）、删除按钮改为悬停显示、卡片悬停出现描边与柔和阴影
- "已划掉"区改为浅灰圆角面板，卡片同步新样式；空列表新增图标式空状态文案
- 列表滚动条替换为纤细圆角样式；样式解析失败时自动回退默认样式，不影响启动
- 设置弹窗同步新视觉（大圆角 + 柔和阴影）

## [1.0.5] - 2026-09-07

### Fixed

- 修复贴入壁纸层后窗口只显示空白背景、整个界面不渲染的问题：贴入后强制 WPF 重建渲染目标（窗口尺寸微调 + 隐藏/显示循环），否则只能看到一片背景色，看起来就像"工具消失了"
- 修复贴入位置可能落在壁纸层范围之外而被裁剪不可见的问题：贴入时自动将窗口收进壁纸层范围内
- 贴入成功的气泡提示补充说明：窗口位于桌面图标与普通窗口的后面，按 Win+D 显示桌面即可看到

## [1.0.4] - 2026-09-07

### Fixed

- 修复 Windows 11 24H2/25H2 上"贴入桌面(壁纸层)"后窗口不可见的问题：新版系统不再生成顶层壁纸 WorkerW（0x052C 已失效），真实壁纸层变为 Progman 下的可见子窗口；现在会优先使用可见壁纸层、始终跳过系统遗留的隐藏 WorkerW（贴入隐藏层会导致窗口"消失"），并在贴入时保持原屏幕位置
- 修复高 DPI 缩放显示器上重新贴入时窗口位置偏移的问题（保存的坐标为 DIP，不应按像素重复应用）

## [1.0.3] - 2026-09-07

### Fixed

- 修复"贴入桌面(壁纸层)"后窗口可能不可见的问题：现在会跳过系统遗留的隐藏壁纸层（如壁纸轮播残留，贴入这种层会导致窗口"消失"），并在贴入后保持原屏幕位置、强制显示窗口

## [1.0.2] - 2026-09-07

### Added

- 设置面板：标题栏新增 ⚙ 按钮，打开设置弹窗，可集中切换"开机自动启动""窗口置顶""贴入桌面(壁纸层)"；开关即时生效并持久化，点击面板外部自动关闭

## [1.0.1] - 2026-09-07

### Added

- 删除待办：每张卡片（待办与"已划掉"区）右侧新增 ✕ 按钮，弹窗确认后彻底删除该条，适合删掉添加错的条目；删除即时持久化，不可恢复
- 应用图标：采用根目录 logo.png 生成 256×256 图标（assets/app.ico），资源管理器、任务栏与托盘统一显示
- 开源：采用 MIT 许可证，仓库公开

## [1.0.0] - 2026-09-07

### Added

- 首个版本发布：Windows 桌面待办清单小组件
- 待办新增：输入 + 回车或"＋ 添加"按钮，截止日期可选
- 勾选划掉：横线划掉动画 + 卡片淡出收起，数据即时持久化
- "已划掉 (N)"折叠区：划掉的条目可随时找回并恢复（↺）
- 时间记录：每条待办记录创建时间与划掉时间
- 排序：有截止日期的按日期升序（过期/当天高亮），无截止日期的按默认顺序置底
- 窗口形态：置顶悬浮（📌切换）与"贴入桌面(壁纸层)"两种模式，位置记忆
- 系统托盘：显示/隐藏、置顶、贴入桌面、开机自启开关、退出
- 开机自启：写入 HKCU Run 注册表键，无需管理员权限
- 数据本地 JSON 持久化：`%APPDATA%\DesktopTodo\todos.json`
- 构建脚本 `build.ps1`：使用系统自带 csc.exe 编译，零外部依赖
